name: trace-guided-data-analysis
description: "Agent 迭代式数据血缘追踪分析工作流 - 每步操作都记录到 OpenTrace 系统，并在观察结果后决定下一步"
version: "1.0"

metadata:
  author: "Claude Opus"
  created: "2026-06-17"
  use_cases:
    - 需要追踪数据来源的分析任务
    - 需要复现和验证的分析流程
    - 需要生成数据血缘报告的项目

phases:
  - title: "初始化会话"
    description: "加载数据并初始化 OpenTrace 会话；只允许完成初始化和首个中间文件写入，不得一次性执行完整分析"
    steps:
      - name: "加载原始数据"
        action: |
          1. 获取 OpenTrace 服务器实例: `server = get_server()`，工作目录使用 `Path(server.base_dir) / session_id`
          2. 初始化会话: `server.init_session(task_description, data_path, data_type)`
          3. 保存原始数据为中间文件: `working_dir/loaded_data.json`
          4. 调用 `server.record_step()` 记录基础步骤
          5. 记录 PROV 关系（包含临时 id）
          6. 调用 `server.record_step_details()` 记录本步代码、命令、输入输出和参数
          7. 读取 `working_dir/loaded_data.json`，总结数据规模、字段和下一步候选方向
        trace:
          entities:
            - id: "raw_data"
              type: "dataset"
              location: "<原始数据路径>"
            - id: "loaded_data"
              type: "dataset"
              location: "<工作目录>/loaded_data.json"
          activities:
            - id: "load_act"
              type: "load"
              description: "加载原始数据"
          agents:
            - id: "data_loader"
              type: "script"
              name: "<步骤名称>"
          relations:
            # 注意：方向非常重要！
            - ("load_act", "raw_data", "used")              # 活动使用输入
            - ("loaded_data", "load_act", "wasGeneratedBy")   # 输出由活动生成
            - ("load_act", "data_loader", "wasAssociatedWith") # 活动关联代理
            - ("loaded_data", "raw_data", "wasDerivedFrom")    # 输出来源自输入

  - title: "Agent 迭代分析"
    description: "逐步分析数据，每次只执行一个明确的数据处理动作；每步结束后必须读取输出、总结发现、再决定下一步"
    steps:
      - name: "执行分析步骤"
        action: |
          对每个分析步骤：
          1. 明确本步问题和输入文件（可以是原始数据、中间结果、或多个文件）
          2. 执行一个小范围数据操作（过滤、聚合、分析、追踪等），禁止一次性脚本完成全部分析
          3. 保存结果为新的中间文件: `step<N>_output.json`
          4. 调用 `server.record_step()` 记录基础步骤
          5. 调用 `server.record_prov_relation()` 记录所有输入输出关系
          6. 调用 `server.record_step_details()` 记录步骤详情（包含生成的代码文件、命令、描述、输入输出、参数）
          7. 读取本步输出文件，检查结果内容
          8. 总结本步发现、异常、限制和可信度
          9. 基于已观察结果决定下一步分析方向；如果结果不足，说明要回溯或补充哪类分析
        trace:
          entities:
            - id: "step_input"
              type: "dataset"
              location: "<上一步输出文件>"
            - id: "step_output"
              type: "dataset"
              location: "<当前步骤输出文件>"
              attributes: {"<key>": "<value>"}
          activities:
            - id: "step_act"
              type: "<filter|transform|analyze|trace>"
              description: "<步骤描述>"
          agents:
            - id: "step_agent"
              type: "script"
              name: "<步骤名称>"
          relations:
            # ⚠️ 顺序和方向很重要！
            - ("step_act", "step_input", "used")              # 活动→输入
            - ("step_output", "step_act", "wasGeneratedBy")   # 输出→活动
            - ("step_act", "step_agent", "wasAssociatedWith") # 活动→代理
            - ("step_output", "step_input", "wasDerivedFrom")  # 输出→输入

  - title: "生成报告和可视化"
    description: "只在迭代分析完成后，基于已经读取和记录过的中间结果生成最终报告和数据血缘可视化"
    steps:
      - name: "汇总结果"
        action: |
          1. 收集所有中间文件的结果
          2. 生成最终分析报告: `final_report.json`
          3. 调用 visualize_prov_dag() 生成可视化
          4. 记录最终汇总的 PROV 关系
        trace:
          entities:
            - id: "step1_out"
              type: "dataset"
              location: "<step1输出文件>"
            # ... 其他步骤的输出
            - id: "final_report"
              type: "artifact"
              location: "<最终报告文件>"
          activities:
            - id: "aggregate_act"
              type: "aggregate"
              description: "汇总所有分析结果"
          agents:
            - id: "report_agent"
              type: "script"
              name: "<报告生成步骤>"
          relations:
            - ("aggregate_act", "step1_out", "used")
            # ... 其他步骤的 used 关系
            - ("final_report", "aggregate_act", "wasGeneratedBy")
            - ("aggregate_act", "report_agent", "wasAssociatedWith")

rules:
  - "禁止一次性 pipeline 脚本直接完成全部分析；每次只能执行一个明确的数据处理步骤"
  - "每步数据操作必须生成中间文件"
  - "每步必须调用 server.record_step() 记录基础步骤"
  - "每步必须调用 server.record_prov_relation() 记录 DAG"
  - "每步必须调用 server.record_step_details() 记录步骤详情"
  - "每步结束后必须读取本步输出文件"
  - "每步结束后必须总结发现、限制和下一步理由"
  - "所有 entities/activities/agents 必须提供临时 id 字段"
  - "明确每步的输入输出文件（数据流转清晰）"
  - "支持读取原始数据、中间结果、或多个文件"
  - "根据上一步结果决定下一步方向（迭代式分析）"
  - "final_report.json 只能在迭代分析完成后生成，且必须基于已记录的中间结果"

output:
  files:
    - "loaded_data.json - 原始数据加载"
    - "step*.json - 各步骤中间结果"
    - "final_report.json - 最终分析报告"
    - "pipeline_visualization.txt - 数据血缘可视化"
  trace_files:
    - "prov_dag.json - DAG 元数据"
    - "prov_nodes.json - 节点信息"
    - "prov_edges.json - 边关系"

example:
  name: "VAST Challenge MC2 分析"
  description: "异常 SaidIT 帖子追踪"
  steps:
    - "加载 MC2 data.json (185,147 事件)"
    - "查找关键 SaidIT 事件 (event 373902)"
    - "追踪 SwiftWren.txt 起源 (191 事件, 18 Agent)"
    - "比较三个异常案例模式"
    - "生成最终报告"
  results:
    - "SwiftWren.txt 由 emma_harbor 创建"
    - "级联传播 7.8 天"
    - "发现 2 个类似历史案例"
    - "建议干预点: SaidIT 发布接口前内容验证"
