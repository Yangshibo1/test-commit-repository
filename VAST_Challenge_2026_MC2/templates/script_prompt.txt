你是一个专业的数据分析专家。请分析以下脚本的核心算法逻辑。

## 背景信息
{background}

## 核心问题
{problems}

## 脚本文件
文件路径: {file_path}

```python
{file_content}
```

## 任务要求

请生成结构化的算法分析：

1. **algorithm_purpose**（算法目标）：简明描述这个脚本的核心目的

2. **algorithm_logic**（算法逻辑说明）：分条描述算法的工作步骤
   - 步骤1：做了什么
   - 步骤2：做了什么
   - 步骤3：做了什么

3. **data_flow**（数据流程）：输入 -> 处理 -> 输出

## 输出格式

输出严格的 JSON 格式，只输出 content 部分。

示例：
{{
  "algorithm_purpose": "根据时间窗口筛选特定日期范围内的事件",
  "algorithm_logic": [
    "读取输入数据集文件",
    "遍历所有事件记录，检查时间戳是否匹配目标日期和时间",
    "筛选出匹配的事件记录",
    "输出筛选结果到JSON文件"
  ],
  "data_flow": "输入: node_02_saidit_events.json (695条) -> 时间过滤 -> 输出: node_03_target_post.json (1条)"
}}

## 重要原则

- 分条描述算法步骤，清晰易懂
- 重点描述"做了什么"，避免技术细节
- 不要提及 OpenTrace、PROV、血缘等技术概念
