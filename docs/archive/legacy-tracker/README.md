# Legacy Tracker 文档

这里保存旧的 `AgentVASTServer`、`LineageTracker`、PROV DAG、`init_session`、
`record_prov_relation` 和多文件 Session 文档。

相关兼容代码目前仍保留在 `agentvast/mcp_server.py`、`agentvast/tracker.py`、
`agentvast/prov_dag.py` 和 `agentvast/step_details.py`，但当前产品入口不使用这些接口。
不要将本目录中的说明注入新的 Claude Code 数据分析任务。
